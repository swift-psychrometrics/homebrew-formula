name: Audit

on:
  pull_request:
    types:
      - opened
  push:
  workflow_dispatch:

jobs:
  audit:
    name: Audit Formula
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Audit
        run: make audit
